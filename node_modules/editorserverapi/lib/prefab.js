"use strict";

var Entity = require('./entity');

/**
* Prefab entity
*
* @class Prefab
* @constructor
* @param {string} root path
* @param {Express} express application
*/
var Prefab = function(_root, _app) {
	Entity.call(this, _root, _app);
};

Prefab.prototype = Object.create(Entity.prototype);
Prefab.prototype.constructor = Prefab;

/**
* Prefab key
*
* @property KEY
* @type string
* @default "prefab"
*/
Prefab.KEY = "prefab";

/**
* Get the entity's path
*
* @method getPath
*/
Prefab.prototype.getPath = function() {
	return this.root + '/' + Prefab.KEY;
};

module.exports = Prefab;